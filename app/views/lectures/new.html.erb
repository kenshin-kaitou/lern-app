<p>動画を投稿する</p>
<%= form_with model: @lecture, local: true do |f| %>
	<%= f.text_field :title %>
	<%= f.file_field :image %>
	<%= f.file_field :video %>
	<%= f.text_area :overview %>
	<%= f.submit %>
<% end %>